<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {# {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %} #}
        <style>
            header{box-shadow: 1px 0px 25px 1px rgb(66, 8, 8);}
            a{color: rgba(248, 0, 0, 0.897)} a:hover{color: rgb(155, 29, 29)}
            .btn-primary{background-color: rgb(155, 5, 5);}
            .btn-primary:hover{background-color: rgb(192, 0, 0);}
            .btn-delete{background-color: rgb(231, 111, 12); color: #FFF}
            .btn-delete:hover{background-color: rgb(199, 98, 15); color: #FFF}
        </style>
    </head>
    <body>

        <header class="d-flex px-5 py-2 mb-5 justify-content-between align-items-center">
            <h1>BNB Baribas</h1>
            <nav class="d-flex">
                <a href="/account" class="mx-2">Mes comptes</a>
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <a href="/logout" class="mx-2">Se déconnecter</a>
                {% else %}
                <a href="/login" class="mx-2">Se connecter</a>
                {% endif %}
            </nav>
        </header>

        {% block body %}{% endblock %}
    </body>
</html>
